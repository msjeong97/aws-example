# Lambda - json_to_parquet_loader

## Description

- This is a python lambda code that receives a kinesis stream as an input, converts specific fields, loads a parquet file to s3, and (optionally) creates a table partition.

